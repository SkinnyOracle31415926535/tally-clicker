{
  "name": "tally-clicker-chatgpt-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --check transfer-theme.js && node --check worker/index.js && node --test tests/private-sync-retirement.test.cjs tests/todo-edit-mode.test.cjs && npm run build"
  }
}
