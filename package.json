{
  "name": "tally-clicker-chatgpt-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --check transfer-theme.js && node --check tally-storage.js && node --check semantic-app-sync.js && node --check worker/index.js && node --test tests/tally-semantic-storage.test.cjs tests/transfer-theme.test.cjs tests/todo-edit-mode.test.cjs tests/private-sync-removal.test.cjs && npm run build"
  }
}
